extends ../layout 
include ../mixins/video

block content 
  form(method="GET")
    input#search(type="text", name="keyword", placeholder="Search by Title")
    button(type="submit") Search Now!

  if(keyword)
    article 
      h4 Search for: #{keyword}
      hr
      each video in videos
        +video(video)
